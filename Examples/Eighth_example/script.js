// Use any data type, return any data type, short circuting

console.log(3 || 'Iverson');
// Using the "OR" operator the first value will be tested and if it is true will be executed and finished the circuit, otherwise the next value will be executed without been evaluated

console.log(3 && 'Iverson');
// Using the "&&" operator the value will be evaliated and if is false will be executed otherwise the next value will be executed
